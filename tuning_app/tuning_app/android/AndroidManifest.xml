<?xml version="1.0" encoding="utf-8"?>
<!--
  This manifest augments Qt's defaults. We explicitly declare the launcher
  activity so the installer shows an enabled OPEN button and the app can be
  launched from the launcher.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.tuning_app">

  <!-- Networking for WebSockets/Network -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <application
    android:label="@string/app_name"
    android:icon="@android:drawable/sym_def_app_icon"
    android:usesCleartextTraffic="true">

    <!-- Qt launcher activity -->
    <activity
      android:name="org.qtproject.qt.android.bindings.QtActivity"
      android:label="@string/app_name"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

  </application>
</manifest>
